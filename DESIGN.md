# 設計書

## 1. 概要

OpenFGAを利用した、ファイルおよびディレクトリの権限を管理するためのシンプルなWebアプリケーションです。
ユーザーはディレクトリ構造（zipファイル）をアップロードでき、アプリケーションはその構造を表現し、権限の管理を可能にします。

## 2. 機能

- **ユーザー認証:** シンプルなユーザー認証（例：メールアドレスとパスワード）。
- **ディレクトリのアップロード:** ユーザーはディレクトリ構造を含むzipファイルをアップロードできます。
- **ディレクトリの可視化:** アプリケーションはディレクトリ構造をツリー形式で表示します。
- **権限管理:**
    - ユーザーは特定のファイルやディレクトリに対して、他のユーザーに権限を付与/剥奪できます。
    - 権限は異なるレベル（例：`reader`, `writer`, `owner`）で定義できます。
- **OpenFGA連携:** アプリケーションはOpenFGAを使用して権限を保存し、チェックします。

## 3. アーキテクチャ

アプリケーションは、主に3つのコンポーネントで構成されます。

- **フロントエンド:** ユーザーインターフェースを提供するNext.jsアプリケーション。
- **バックエンド:** フロントエンドにAPIを提供するGoアプリケーション。
- **OpenFGA:** 認可を管理するためのOpenFGAサーバー。

### 3.1. フロントエンド

- **フレームワーク:** Next.js (React)
- **UIライブラリ:** Material-UI または同様のもの。
- **主要ページ:**
    - ログイン/登録
    - ダッシュボード（アップロードされたディレクトリ構造を表示）
    - ディレクトリビュー（ディレクトリ構造のツリービューを表示）
    - 権限管理UI

### 3.2. バックエンド

- **言語:** Go
- **フレームワーク:** Gin または同様のもの。
- **データベース:** ユーザー情報とディレクトリ構造のメタデータを保存するためのシンプルなデータベース（例：SQLiteまたはPostgreSQL）。
- **REST APIエンドポイント:** (フロントエンドとの通信はJSON形式で行います)
    - `POST /register`: 新規ユーザー登録
    - `POST /login`: ユーザーログイン
    - `POST /upload`: ディレクトリ構造（zipファイル）のアップロード
    - `GET /directories`: アップロードされたディレクトリ構造のリストを取得
    - `GET /directories/:id`: ディレクトリ構造の詳細を取得
    - `POST /permissions`: 権限の付与
    - `DELETE /permissions`: 権限の剥奪

### 3.3. OpenFGA

- **認可モデル:**
    - 認可モデルは`.fga`ファイルで定義されます。
    - モデルは`user`, `directory`, `file`の型を定義します。
    - `owner`, `writer`, `reader`のようなリレーションを定義します。

- **タプル:**
    - ユーザーがディレクトリ構造をアップロードすると、バックエンドはOpenFGAに対応するオブジェクトを作成します。
    - 例えば、ユーザー`user:alice`がディレクトリ`directory:mydocs`をアップロードすると、バックエンドはタプル`(user:alice, owner, directory:mydocs)`を作成します。

## 4. 開発計画

1. **OpenFGAのセットアップ:** Dockerを使用してOpenFGAサーバーをセットアップします。
2. **バックエンド開発:**
    - RESTful APIを実装します。
    - ユーザー認証を実装します。
    - ディレクトリのアップロード機能を実装します。
    - OpenFGA SDKと連携して、タプルの作成と権限のチェックを実装します。
3. **フロントエンド開発:**
    - ユーザー認証のUIを実装します。
    - ディレクトリのアップロードと可視化のUIを実装します。
    - 権限管理のUIを実装します。
